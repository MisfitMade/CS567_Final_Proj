******
To move certain files from one folder to another, be in
the source folder and create the "dest" folder path, then
******

*** To move files of a certain size ***
find . -type f -size +47c -exec mv {} path/to/dest/ \;

*** To move files that start with the numbers 1 - 31 and a _ ***
for n in {1..31}; do for file in "$n"_*; do mv "$file" path/to/dest/; done

*** To move files that start with "Update_"
for file in Update_*; do mv "$file" path/to/dest/; done

*** To append ".json" to the name of every file in your current folder ***
find . -type f -exec mv '{}' '{}'.json \;
